# Sorry about this - I did not make any effort to make it readable

Benchmark Parse mode:

$ java -cp "*:." Bench && java -cp "*:." Bench parse

Benchmark Zero Copy mode:

$ java -cp "*:." Bench && java -cp "*:." Bench zc
